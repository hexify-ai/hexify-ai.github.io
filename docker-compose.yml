version: "3.8"
services:
  jekyll:
    image: bretfisher/jekyll-serve
    volumes:
      - .:/site
    ports:
      - "5000:4000"
      - "35729:35729"
    command: bundle exec jekyll serve --force_polling -H 0.0.0.0 -P 4000
